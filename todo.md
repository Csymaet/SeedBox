# 待办事项 (TODO)

1. 使用什么内容涌现式编号，并写卡片
2. 添加数据卡片，记录每位角色的出生日期和时间，有点仪式感

## 自动化知识卡片引用管理 (方案三)

1.  **维护映射表：** 创建并维护一个内部文件（例如 `_card_map.json`），记录每个知识卡片的当前编号和标题的映射关系。
2.  **自动更新引用：**
    *   当知识卡片被重命名（编号改变）时，首先更新映射表。
    *   然后，运行一个脚本，扫描所有引用了该卡片的文件，根据映射表自动更新引用。
3.  **引用校验：** 定期检查所有引用，确保它们都指向有效的知识卡片。
